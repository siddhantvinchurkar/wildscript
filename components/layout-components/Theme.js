import { createMuiTheme } from "@material-ui/core/styles";

export default createMuiTheme({
  palette: {
    primary: {
      main: "#008000",
    },
    secondary: {
      main: "#FFFFFF",
    },
    error: {
      main: "#FF0000",
    },
    background: {
      default: "#FFFFFF",
    },
  },
});
