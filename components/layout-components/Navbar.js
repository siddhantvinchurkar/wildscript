import React from "react";
import Typography from "@material-ui/core/Typography";

const Navbar = () => {
  return (
    <React.Fragment>
      <Typography variant="h2">Navbarydoo</Typography>
    </React.Fragment>
  );
};

export default Navbar;
